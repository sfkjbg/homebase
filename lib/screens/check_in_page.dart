
import 'package:flutter/material.dart';

class CheckInPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Check In'),
      ),
      body: Center(
        child: Text('Check In Page Content'),
      ),
    );
  }
}